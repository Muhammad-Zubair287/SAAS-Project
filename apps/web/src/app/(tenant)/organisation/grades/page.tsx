import { GradesPageClient } from './grades-page-client';

export default function OrganisationGradesPage() {
  return (
    <GradesPageClient
      title="Grades"
      description="Manage organisation grade catalog."
    />
  );
}
